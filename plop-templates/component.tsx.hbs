import React from "react";
import Styled{{name}} from "./{{name}}.styles";

export interface {{name}}Props {
  label: string;
}

const {{name}} = (props: {{name}}Props) => {
  return <Styled{{name}}>{props.label}</Styled{{name}}>;
};

export default {{name}};